server:
  port: 8081

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/techshop_userdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh&createDatabaseIfNotExist=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  jackson:
    serialization:
      write-dates-as-timestamps: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
        max.block.ms: 5000

# Outbox Pattern config
outbox:
  publisher:
    poll-interval-ms: 5000
    batch-size: 50
    max-retries: 10
    kafka-timeout-seconds: 5
  cleanup:
    interval-ms: 3600000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

jwt:
  secret: ${JWT_SECRET:techshop-super-secret-key-change-in-production-2024}
  expiration: ${JWT_EXPIRATION:86400000}

oauth2:
  google:
    client-id: ${GOOGLE_CLIENT_ID:google-mock-client-id}
    client-secret: ${GOOGLE_CLIENT_SECRET:google-mock-client-secret}
    redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:5173/oauth2/callback/google}
  facebook:
    client-id: ${FACEBOOK_CLIENT_ID:facebook-mock-client-id}
    client-secret: ${FACEBOOK_CLIENT_SECRET:facebook-mock-client-secret}
    redirect-uri: ${FACEBOOK_REDIRECT_URI:http://localhost:5173/oauth2/callback/facebook}

logging:
  level:
    com.techshop.userservice: DEBUG
    org.springframework.security: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n"
